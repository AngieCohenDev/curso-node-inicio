const nombre = 'Deadpool';
const real = 'Wide Wilson';

const normal = nombre + ' ' + real;
const template = `${normal} ${real}`;
console.log(template);

console.log(normal);

const html = `<h1> HOla mundo</h1>
<p>ESte es el console.log</p>`

console.log(html);